{
  "name": "glsl-docs-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for GLSL, Three.js, and React Three Fiber shader documentation",
  "type": "module",
  "main": "dist/glsl-docs-server.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/glsl-docs-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
